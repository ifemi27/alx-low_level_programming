#include <stdio.h>

/**
 * main - prints numbers in base ten
 * Return: Always 0 (Success)
 **/
int main(void)
{
	int n;

	for (n = 48; n < 59; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
